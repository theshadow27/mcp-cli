# 2026-03-18 — Test Debt + defineAlias + ACP Spike + Clean Sweep #2 (Sprint 17)

## What was done

16 PRs merged, second consecutive clean sweep.

**Sprint 16 follow-ups (3 PRs):**
- serve.ts coverage (#824, PR #843) — brought above 80% threshold
- Remaining setTimeout waits (#816, PR #840) — all bare setTimeout removed from use-plans.spec.ts
- Dead usePlan hook (#776, PR #839) — deleted unused hook, interfaces, and test file

**Flaky test fixes (2 PRs):**
- Idle timeout daemon-integration test (#487, PR #838) — increased timeout, added diagnostic logging. Opus adversarial review caught 2 issues, repaired.
- (Also #807 and #809 from Sprint 16 — those were in the pipeline from last sprint)

**defineAlias deepening (2 PRs):**
- Zod schema validation for aliases (#94, PR #841) — validation on save and run. High scrutiny: adversarial review found 2 P0s (output validation, schema extraction), repaired.
- cache() helper for defineAlias (#752, PR #848) — `cache(key, producer, opts?)` on AliasContext with TTL and file-backed persistence.

**CLI first-run polish (3 PRs):**
- Import from Claude Desktop config (#77, PR #844) — `mcx import --claude-desktop`
- First-run prompt when .mcp.json detected (#86, PR #846) — prompts import on first command
- Tab completion for mcx install (#80, PR #854) — `--registry` flag with slug completion

**Daemon stability (2 PRs):**
- Stdio retry semantics (#39, PR #850) — distinguish crash vs startup failure in server-pool
- Integration tests for transport error messages (#38, PR #856)

**ACP/OpenCode spikes (2 PRs):**
- ACP spike with Copilot CLI (#518, PR #853) — validated full ACP protocol end-to-end. User manually tested with `gh copilot --acp`. Fixed `Bun.openFile` → `Bun.file()` and session/new mcpServers requirement. Filed #857 (binary resolution for copilot).
- OpenCode spike (#503, PR #860) — validated OpenCode server protocol from Bun.

**TUI + docs (2 PRs):**
- mcpctl serve instance tracking (#148, PR #859) — new IPC methods for serve instance registration, TUI server list shows connected clients
- Sprint skill docs (#834, PR #836) — README pointer to sprint skill

**Also closed during planning:** #804 (resolved by #812), #700 (Plans tab epic complete), #131 (superseded), #719 (resolved by #718)

## What worked well

- **Second clean sweep (16/16)**: Back-to-back perfect sprints. 31 PRs across Sprints 16-17 with zero drops.
- **Pre-commit tiers delivering value**: Docs, sprint files, config changes all commit instantly. The tiered system from Sprint 15 continues to pay off.
- **Hash-based timing cache working**: First full cache build happened this sprint (55s initial, incremental afterward). No more blind re-timing.
- **ACP spike validated by actual user**: First time a human manually tested a sprint deliverable. Copilot integration works — ACP SDK is viable for the implementation phase.
- **Codex tried to fix its own bug**: Codex session attempted to repair #851 (threadId missing in spawn). Work saved to `fix/codex-threadid-851` branch. Meta.

## What didn't work

- **2 sessions stuck in "connecting" on daemon auto-start**: Sessions spawned immediately after daemon startup never connected. Filed #837. Likely regression from #718 (WS port reclaim dual-server). Workaround: respawn after a few seconds.
- **`mcx codex spawn` broken (#851)**: Missing `threadId` field. Codex can't be used for reviews until fixed. The Codex self-repair attempt produced valid code but couldn't test due to binary mismatch.
- **4 duplicate coverage issues filed again**: Same pattern as Sprint 16 — multiple sessions independently file the same coverage issue. Need dedup instruction in implement skill.
- **OpenCode spike lost on first attempt**: Session `ebe98494` completed but worktree was auto-cleaned on bye before PR was pushed. Had to respawn. Same root cause as Sprint 15's worktree lifecycle issue.
- **ACP spike needed user fix**: `Bun.openFile` is not a real Bun API. Implementation sessions should validate Bun APIs exist before using them.

## Patterns established

- **Codex work preserved on branches**: When Codex/other agents modify files, save to a branch before cleaning up. Don't discard uncommitted work.
- **Don't spawn sessions immediately after daemon auto-start**: Wait for `_claude` to show "connected" in `mcx status` first.
- **User manual testing for spikes**: ACP spike was the first sprint issue manually validated by the user. Spikes benefit from human testing — automated QA can't validate external tool integration.

## Stats

- **PRs merged**: 16
- **Issues closed**: 20 (16 merged + 4 during planning)
- **Adversarial reviews**: 2 (#94: 2 P0s, #487: 2 required changes)
- **Repair rounds**: 3 (#94 x1, #487 x1, #518 x1)
- **Failed/dropped**: 0
- **Follow-ups filed**: #833 (release formatting), #835 (ls auto-start), #837 (connecting race), #842 (idle timeout root cause), #851 (codex spawn), #857 (copilot binary), #862 (agents tab)
- **Sprint wall time**: ~3.5 hours (including user ACP testing)

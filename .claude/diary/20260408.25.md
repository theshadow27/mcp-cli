# Sprint 25 Retro — 2026-04-08

## What happened

Sprint 25 fixed the pre-commit pipeline and test stability. 14 PRs merged (13 planned + 1 customer request). v1.1.2 released. The three P1 pre-commit fixes (#1078, #1085, #1084) mean sessions should no longer need `--no-verify` — though ironically every session this sprint still needed it because the fixes weren't on their branches yet.

## Pre-commit fixes

- **#1078**: Daemon test deadline raised from 120s to 300s
- **#1085**: Skip daemon tests in pre-commit when no daemon files staged (fast path)
- **#1084**: CI auto-retries on Bun segfault exit code 132

These will be validated in Sprint 26 — the first sprint where the fixes are on main before sessions fork.

## Worktree pollution (#1109)

7 files in the main repo were modified by spawned sessions — up from 3 in Sprint 21. `git checkout -- .` doesn't fix them (requires `git reset --hard`). Filed #1109 with forensics. This is P2 but getting worse.

Hypothesis: pre-commit hook or test processes resolve cwd to the main repo via the worktree's `.git` file. Need a nerdsnipe to investigate.

## What worked

- **All 13 issues spawned and QA'd in one pass** — no review-repair cycles needed for the test fixes
- **Customer request (#1106) tacked on smoothly** — xxxs help text trim, done in 20 min
- **CI retry (#1084)** — should reduce the 33% Bun segfault false-failure rate to near zero
- **`wait` + `ls` idle detection working** — confirmed on v1.1.0+ build, no more `sleep + ls` polling hacks

## What didn't work

- **Every session still needed `--no-verify`** — the pre-commit fixes merged during the sprint but sessions forked before they landed. Next sprint will be the real test.
- **Worktree pollution escalating** — 3 files (Sprint 21) → 7 files (Sprint 25). Needs investigation before it causes a real problem (accidentally committing worktree changes to main).
- **QA sessions taking long** — several QA sessions ran full test suites (3800+ tests, 150s each). The pre-commit fast path (#1085) should help since QA only modifies test files.

## Numbers

- **14 PRs merged** (13 planned + 1 customer)
- **3 P1 pre-commit fixes** shipped
- **7 flaky tests fixed** + 2 smoke test expansions
- **0 adversarial reviews** needed — all issues were well-scoped with clear fixes
- **Worktree pollution:** 7 files (filed #1109)

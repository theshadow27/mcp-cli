# 2026-03-19 — OpenCode + Distribution + v1.0 Path Cleared (Sprint 19)

## What was done

15 PRs merged (14 real + #882 already implemented), fourth consecutive clean sweep (75 total across Sprints 15-19).

**OpenCode Agent Provider (2 PRs):**
- Full `packages/opencode/` implementation (#504, PR #896) — OpenCode SSE client, session management, event mapping, permission adapter, transcript capture. Adversarial review found critical stdout reader lock issue, repaired.
- `mcx opencode` CLI commands (#505, PR #898) — full CLI parity: spawn, ls, wait, send, bye, log, interrupt.

**ACP Integration Completion (2 PRs):**
- Gemini install hint fix (#878, PR #887)
- Skills `--provider` flag for ACP routing (#883, PR #890)

**Distribution (3 PRs):**
- npm publishing setup (#36, PR #886) — scoped packages, publish workflow
- Homebrew tap + release automation (#37, PR #889) — tap repo, formula template, CI job
- `mcx update` command (#79, PR #891) — version check against registry

**defineAlias Arc Completion:**
- Alias composition with typed cross-alias calls (#104, PR #893) — executor connects back to daemon via IPC for real tool calls

**Infrastructure + DX (7 PRs):**
- CI build fix (#884, PR #885) — deferred intermediate bundle cleanup to prevent race
- SSE endpoint for streaming daemon logs (#41, PR #892)
- OAuth auto-discover from Claude Code keychain (#113, PR #894)
- Notes system: per-tool annotations (#12, PR #901) — `mcx note set/get/list`
- HTTP/SSE transport integration tests (#115, PR #900)
- Test suite wall time optimization (#690, PR #895)
- #882 (ACP virtual server) — already implemented in Sprint 18, verified and closed

## What worked well

- **Fourth consecutive clean sweep (15/15)**: 75 PRs across 5 sprints with zero drops. The pipeline is battle-tested.
- **OpenCode shipped in one sprint like ACP**: Same pattern — spike → provider → CLI. The `AgentSession` interface made the second provider much faster to implement.
- **Distribution ready for v1.0**: npm, homebrew, and `mcx update` all landed. First time the project has real distribution channels.
- **Ancient issues closed**: #12 (notes, filed Sprint 1), #36/#37 (distribution, phase 5), #41 (SSE logs), #104 (alias composition) — some of these were 9 sprints old.

## What didn't work

- **#504 QA took 91 turns**: The OpenCode provider QA session was thorough but slow, spending a lot of time on CI verification. May need a faster QA path for already-reviewed code.
- **#12 (notes) cost $14.20**: Expensive for a feature that could have been simpler. The implementation likely over-engineered the storage layer.

## Patterns established

- **Provider implementation is now a template**: ACP → OpenCode → future providers follow the same pattern: `packages/{name}/`, AgentSession interface, event map, permission adapter, transcript, process management, fake agent for tests.
- **Distribution is a CI concern, not a code concern**: npm publish and homebrew tap are workflow additions, not application code changes.

## Stats

- **PRs merged**: 15 (14 new + 1 already done)
- **Issues closed**: 15
- **Adversarial reviews**: 1 (#504: stdout reader lock, repaired)
- **Repair rounds**: 1 (#504)
- **Failed/dropped**: 0
- **Sprint wall time**: ~5 hours
- **Cumulative across Sprints 15-19**: 75 PRs, 1 deferred (Sprint 15), 4 consecutive clean sweeps
- **Board after sprint**: ~14 open issues — v1.0 territory

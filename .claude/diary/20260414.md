# 2026-04-14 — Catch-up Pull After 96-Commit Gap

## What was done

### Session 14757e2e — pull, survey, rebuild
- Fast-forwarded from `dee6dfc` to `6f3f144` (v1.5.0-rc.1); 96 commits, ~25k additions / ~800 deletions across 183 files
- New tags picked up: v1.3.0, v1.3.1, v1.4.0, v1.5.0-rc.1
- Rebuilt all three binaries (`dist/mcpd`, `dist/mcx`, `dist/mcpctl`); protocol hash `8071f2608a29`
- Deps unchanged after pull

### Major themes surveyed in the catch-up
- **Declarative phases system (sprint 33)** — `.mcx.{json,yaml}` manifest + loader (#1305), `mcx phase install` + `.mcx.lock` (#1320), source URI parser (#1303), `show`/`list`/`why` inspection (#1340), `phase log` audit (#1373), transition graph + `--force` (#1322), lockfile drift detection (#1338), dry-run via logging Proxy on `ctx.mcp` (#1333), `--arg key=val` forwarding (#1387), runsOn branch guard (#1321), serialized transition log with O_EXCL (#1368)
- **git-remote-mcx epic (sprint 32)** — argv[0] dispatch + install symlink (#1271), fast-import stream writer (#1257) + parser (#1273), git remote helper line protocol (#1228), ported git t5801 test suite (#1276), `mcx gc` (#1278), `--depth` shallow clone (#1239), VFS auto-sets `mcx://` on clone; `vfs push/pull` deprecated (#1270)
- **VFS providers** — GitHub Issues (#1231), Jira Issues (#1201), Asana Tasks (#1202); first-run setup + rate-limit retry + error classification (#1229)
- **Tracing/telemetry** — W3C span hierarchy with causal chain (#1236), `_tracing` virtual MCP server (#1238), usage analytics (#1237)
- **Orchestrator DX** — `mcx wait --any` races session + work-item events (#1186), `--mail-to` surfaces mail events

## What worked well

1. Fast-forward was clean — no rebase drama despite the gap
2. `git log --stat` + filtering by conventional-commit prefix gave a readable summary of a 96-commit window without reading every message

## What didn't work

1. **`bun build` with no entrypoint fails** — must invoke `bun run build` to hit the package.json script. Cheap papercut but wasted a step

## Patterns established

- Use `bun run build` (not bare `bun build`) when rebuilding binaries — the former hits the package script, the latter expects an entrypoint arg

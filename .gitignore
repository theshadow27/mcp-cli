node_modules/
dist/
*.tsbuildinfo
.DS_Store

# State directory (local to each machine)
# ~/.mcp-cli/ is the runtime state dir, not in repo

# Build artifacts
*.js.map
build/
.claude/worktrees/*
.env
test-timing-report.json

# 2.1.132 — deep dive (last 4-occurrence version)

**Verdict:** v1's transform neutralizes the gate. fedstart count = **4**.
The 4 occurrences are **2 live source-array literals** (`LN_`, one per bundle
copy) + **2 dead string-table atoms**. The binary is *doubly-bundled*: the
entire host-check chunk (gate fn + Set builder + `LN_` array) appears twice.

## fedstart offsets (from scan.json) and what each is

| # | offset    | kind                  | evidence |
|---|-----------|-----------------------|----------|
| 0 | 73049207  | live `LN_` array, copy #1 | `LN_=[` @73049120, immediately followed by `]});function _j4(){...` |
| 1 | 147766392 | dead string-table atom | bracketed by raw length-prefix bytes, not JS; not referenced by any `.map` |
| 2 | 147766632 | dead string-table atom | same — second interned atom of the same string |
| 3 | 198151295 | live `LN_` array, copy #2 | `LN_=[` @198151208, byte-identical window to copy #1 |

The two source-array windows (`73049120` and `198151208`) `diff` **IDENTICAL**.

## The LIVE gate (the load-bearing finding)

Located by slicing around the single *source-code* `api-staging.anthropic.com`
occurrence (offset 84361804). The gate is function `A0K`, allowlist Set `hZ3`:

```js
function A0K(H){
  let _;
  try{_=new URL(H)}catch{return `could not parse ${qD6(H)} as a URL`}
  if(hZ3.has(_.hostname)){
    if(_.protocol!=="wss:"&&_.protocol!=="https:")
      return `scheme ${qD6(_.protocol)} is not permitted for host ${qD6(_.hostname)}; only wss:// and https:// are accepted`;
    return null;            // <-- host approved, no error
  }
  return `host ${qD6(_.hostname)} is not an approved Anthropic endpoint`;
}
var qD6=(H)=>JSON.stringify(H),hZ3;
var z0K=Z(()=>{ w4();
  hZ3=new Set([
    "api.anthropic.com",
    "api-staging.anthropic.com",
    ...LN_.map((H)=>new URL(H).hostname)   // <-- consumes the LN_ array literal
  ]);
});
```

`LN_` (defined adjacent to the unrelated chalk function `_j4`) is:
```js
LN_=["https://beacon.claude-ai.staging.ant.dev",
     "https://claude.fedstart.com",
     "https://claude-staging.fedstart.com"]
```

### Gate flow
`A0K(sdkUrl)` → `new URL(sdkUrl).hostname` must be in `hZ3`. `hZ3` =
{api.anthropic.com, api-staging.anthropic.com} ∪ hostnames of the three `LN_`
origins = {beacon.claude-ai.staging.ant.dev, claude.fedstart.com,
**claude-staging.fedstart.com**}. Five hosts total.

### Note on `_j4` (kills part of the orchestrator hypothesis)
`_j4` is **not** the allowlist builder — it is the **chalk/ansi-colors** module
(`let H=new Map; ... H.set(O[0],O[1])` over ansi code tables). It merely sits
*textually adjacent* to the `LN_=[...]` array in the bundle. The real builder is
`z0K`/`hZ3`. The orchestrator's "`_j4` builds the allowlist Map" was a
mis-identification from adjacency; the de-dup conclusion still holds, just via
the correct symbol (`z0K`/`hZ3`/`LN_`).

## Doubled-bundle proof

| signature                                    | count in .132 |
|----------------------------------------------|---------------|
| `LN_=[`                                       | 2 |
| `LN_.map`                                      | 2 |
| `hZ3=new Set`                                  | 2 |
| gate code close `...approved Anthropic endpoint\`}` | 2 |

Two complete, byte-identical copies of the host-check chunk. Each copy has its
own `LN_` array and its own `hZ3` Set builder.

## v1 transform result (empirical)

```
2.1.132: replacementCount=4, liveArrayLiteralReplaced=2, canonHostname=[::1]
```

After v1: both `LN_` copies become
`...claude.fedstart.com","https://[000:000:000:000:000:0:0:1]"]`, and
`new URL("https://[000:000:000:000:000:0:0:1]").hostname === "[::1]"` (WHATWG).
So `[::1]` joins `hZ3`. `ws://[::1]:PORT` passes `A0K`. v1 validates (count==4).

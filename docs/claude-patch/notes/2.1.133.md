# 2.1.133 — deep dive (first 3-occurrence version)

**Verdict:** v1's transform STILL neutralizes the gate. fedstart count = **3**.
The 3 occurrences are **1 live source-array literal** (`QN_`) + **2 dead
string-table atoms**. The 4→3 drop is pure **de-duplication**: the previously
doubly-bundled host-check chunk is now bundled **once**. The binary also shrank
218MB→204MB and `api.anthropic.com` dropped 120→69 — a bundler/minify rebuild
that collapsed the duplicate chunk and renamed all the minified identifiers.

## fedstart offsets (from scan.json) and what each is

| # | offset    | kind                  | evidence |
|---|-----------|-----------------------|----------|
| 0 | 134000920 | dead string-table atom | bracketed by raw length-prefix bytes, not JS |
| 1 | 134001160 | dead string-table atom | same — second interned atom |
| 2 | 184422821 | live `QN_` array (only copy) | `QN_=[` @184422734, followed by `]});function GD4(){...` |

`QN_=[` count = **1**, `QN_.map` count = **1**, gate builder count = **1**.

## The LIVE gate (renamed but structurally identical to .132's `A0K`/`hZ3`)

Located by slicing around the sole `new Set(["api.anthropic.com` occurrence
(offset 195755952). Gate fn `B0K`, allowlist Set `$k3`:

```js
function B0K(H){
  let _;
  try{_=new URL(H)}catch{return `could not parse ${CD6(H)} as a URL`}
  if($k3.has(_.hostname)){
    if(_.protocol!=="wss:"&&_.protocol!=="https:")
      return `scheme ${CD6(_.protocol)} is not permitted for host ${CD6(_.hostname)}; only wss:// and https:// are accepted`;
    return null;
  }
  return `host ${CD6(_.hostname)} is not an approved Anthropic endpoint`;
}
var CD6=(H)=>JSON.stringify(H),$k3;
var U0K=Z(()=>{ X4();
  $k3=new Set([
    "api.anthropic.com",
    "api-staging.anthropic.com",
    ...QN_.map((H)=>new URL(H).hostname)   // consumes QN_
  ]);
});
```

`QN_` (defined adjacent to chalk fn `GD4` — the renamed `_j4`):
```js
QN_=["https://beacon.claude-ai.staging.ant.dev",
     "https://claude.fedstart.com",
     "https://claude-staging.fedstart.com"]
```

### Rename map .132 → .133
| .132 | .133 | role |
|------|------|------|
| `A0K` | `B0K` | gate function (consults the Set) |
| `hZ3` | `$k3` | allowlist `Set<hostname>` |
| `qD6` | `CD6` | `JSON.stringify` helper |
| `z0K` | `U0K` | the Set-builder thunk |
| `LN_` | `QN_` | the fedstart-origins source array |
| `_j4` | `GD4` | chalk/ansi module (NOT the allowlist — adjacency only) |

Logic is **identical** — same `new URL().hostname` allowlist, same scheme check,
same 5-host shape (2 hardcoded + 3 from the origins array).

## Single-bundle proof (de-dup confirmed)

| signature                                    | .132 | .133 |
|----------------------------------------------|------|------|
| `<fedstart-origins>=[` (`LN_`/`QN_`)          | 2    | 1    |
| `.map` into the Set                           | 2    | 1    |
| `new Set` allowlist builder                   | 2    | 1    |
| gate code close `...approved Anthropic endpoint\`}` | 2    | 1    |
| fedstart total                                | 4    | 3    |

The two string-table atoms are unchanged (still 2 in both versions). Only the
*executable JS copy count* dropped from 2→1. 4 − 1 = 3. Exactly matches.

## v1 transform result (empirical)

```
2.1.133: replacementCount=3, liveArrayLiteralReplaced=1, canonHostname=[::1]
```

The single live `QN_` array becomes
`...claude.fedstart.com","https://[000:000:000:000:000:0:0:1]"]`. The builder
`U0K` runs `new URL("https://[000:000:000:000:000:0:0:1]").hostname` = `[::1]`,
adding it to `$k3`. `B0K("ws://[::1]:PORT")` → `$k3.has("[::1]")` true, scheme
`wss:`/`https:` check (mcx uses `ws:`/`wss:` — see note below). `[::1]` is an
approved host. **The transform works; only v1's `validate` count assertion
(`===4`) is wrong.**

### Caveat to verify downstream (not a blocker for the count fix)
The scheme branch only allows `wss:`/`https:`. mcx's daemon URL is
`ws://[::1]:PORT`. v1 was validated end-to-end at .121/.123, so either mcx uses
`wss://` to the daemon or the scheme check path differs from what's shown — this
is unchanged .132→.133 (same `protocol!=="wss:"&&protocol!=="https:"` in both),
so it is NOT part of the 4→3 regression and does not affect the count fix.

# 2.1.133 (range-stability cross-check — see also 2.1.133.md owned by another agent)

**Verdict:** v1 `apply` neutralizes the gate; count=3 (1 live JS array literal +
2 inert atoms). First version of the 3-occurrence era. Mechanism identical to the
count=4 reference (2.1.132) minus the de-duplicated second module copy.

## Evidence
- fedstart offsets / classification:
  - @134000920 ATOM, @134001160 ATOM, @184422821 LIVE JS array literal.
- Source array (var `QN_`, @184422734):
  `["https://beacon.claude-ai.staging.ant.dev","https://claude.fedstart.com","https://claude-staging.fedstart.com"]`
- Allowlist Set builder (@195755952):
  `$k3=new Set(["api.anthropic.com","api-staging.anthropic.com",...QN_.map((H)=>new URL(H).hostname)])`
- Validator `GD4`... wait — validator is the fn returning `is not an approved
  Anthropic endpoint`. (Adjacent `GD4` after the array is the ansi-styles Map
  builder, NOT the allowlist.) The live validator + gate match the range shape:
  `VALIDATOR(H){...if(SET.has(_.hostname)){if scheme!=wss/https reject; return null}
  return "host ... is not an approved Anthropic endpoint"}` and gate
  `let r=VALIDATOR(arg); if(r!==null) ...--sdk-url rejected: ${r}...`.
- 218→204MB shrink + hostProd 120→69 at this version = bundler rebuild that
  de-duplicated the doubly-bundled ARR module (4→3 fedstart). Pure de-dup.
